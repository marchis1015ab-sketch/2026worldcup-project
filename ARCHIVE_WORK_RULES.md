[공통 주의사항]
기존 기능, UI, 저장 데이터가 뒤틀리지 않게 최소 범위로 수정한다.
기존 자료를 삭제하거나 overwrite하지 말고 append/merge 방식으로 처리한다.
PC/모바일/태블릿 레이아웃을 모두 확인하되, 이번 요청 범위 밖 레이아웃은 절대 수정하지 않는다.
stale cache, mock/seed 데이터, localStorage 복원 문제로 삭제된 데이터가 다시 나타나지 않게 한다.
수정 후 저장/삭제/업로드/새로고침/배포 최신 여부까지 검증한다.

[이번 작업 목표]
보관함 안의 문서보관 / 파일보관 / 갤러리 표시 방식을 정리한다.
이번 작업은 보관함 표시 방식 정리와 보관자료 미리보기 분할 구조 제거만 한다.
불필요한 최적화, 구조개편, 새 기능 추가, 다른 탭 수정, 모바일 수정, 전역 CSS 수정은 하지 않는다.

[절대 금지]
- 모바일 레이아웃 건드리지 말 것.
- 태블릿 레이아웃 건드리지 말 것.
- 공용일정, 개인일정, 장비, 방송편성, MAP, 경기일정 로직 건드리지 말 것.
- Supabase 저장/조회/삭제 로직 건드리지 말 것.
- gallery/storage/upload 저장 구조 바꾸지 말 것.
- 기존 데이터 삭제/초기화/덮어쓰기 금지.
- 새 브랜치, 새 폴더, 새 preview, 새 실험 구조 만들지 말 것.
- 요청하지 않은 리팩토링/최적화/정리 작업 하지 말 것.
- “겸사겸사 개선” 금지.
- 모바일까지 같이 손보는 헛짓거리 하지 말 것.
- 보관자료 미리보기 분할 구조를 유지하거나 새 분할 패널로 재구성하지 말 것.
- 기존 버튼/이벤트/함수 연결을 끊지 말 것.

[수정 범위]
보관함 관련 UI 표시만 수정한다.

1. 문서보관
- 한 줄 목록형으로 표시한다.
- 문서명 / 날짜 / 메모 / 열기 / 삭제 같은 정보를 한 줄에 정리한다.
- 무거운 미리보기보다 빠른 목록 표시를 우선한다.
- 기본 화면에서 큰 미리보기 패널을 펼쳐놓지 않는다.
- 기존 문서 저장/삭제/불러오기 기능은 유지한다.
- 기존 파일 데이터와 Supabase 데이터 구조는 변경하지 않는다.

2. 파일보관
- 카드 형식으로 표시한다.
- 파일명, 종류, 날짜, 메모, 미리보기/다운로드 영역을 카드 안에 배치한다.
- 뉴슈트 패널 느낌에 맞게 정리하되, 기존 사이트 전체 디자인을 새로 해석하지 않는다.
- 카드 크기는 과하게 키우지 말고, 한 화면에 여러 개가 보이도록 정리한다.
- 기존 파일 저장/삭제/다운로드/미리보기 기능은 유지한다.

3. 갤러리
- 이미지/영상 썸네일 중심으로 표시한다.
- 여러 파일 동시 업로드 기능은 유지한다.
- 업로드 후 새로고침해도 유지되어야 한다.
- 공용일정 첨부파일 자동 저장과 충돌 없게 유지한다.
- 갤러리 저장 로직, storage bucket, 기존 데이터는 건드리지 않는다.

4. 보관자료 미리보기 영역
- 보관자료 미리보기가 좌우/상하로 분할되어 보이는 구조를 없앤다.
- 미리보기 화면을 여러 칸으로 쪼개지 말고, 선택한 자료 하나를 중심으로 단일 미리보기 영역으로 정리한다.
- 문서보관은 한 줄 목록형을 우선하고, 무거운 미리보기 패널을 기본으로 펼치지 않는다.
- 파일보관은 카드 안에서 필요한 미리보기/다운로드만 제공하고, 별도의 분할 미리보기 레이아웃은 제거한다.
- 갤러리는 썸네일 중심으로 유지하되, 기존 업로드/저장/삭제/새로고침 유지 기능은 절대 건드리지 않는다.
- 미리보기 분할 제거는 PC 보관함 표시 영역에만 적용하고, 모바일 레이아웃은 건드리지 않는다.

[작업 방식]
- 먼저 현재 보관함 관련 DOM/class/function 위치를 확인한다.
- 필요한 최소 CSS/HTML/JS만 수정한다.
- 모바일 media query 안의 코드는 수정하지 않는다.
- max-width 767px, 768~1024px 관련 CSS는 건드리지 않는다.
- PC 보관함 표시가 목적이면 PC 범위에만 제한해서 수정한다.
- 기존 함수명을 바꾸지 말고, 기존 이벤트 연결을 끊지 않는다.
- 삭제 버튼, 열기 버튼, 다운로드 버튼, 미리보기 버튼의 기존 동작을 유지한다.
- 기존 자료가 없는 것처럼 보이게 만드는 필터/초기화/빈 상태 강제 처리를 추가하지 않는다.
- 기존 저장 데이터 구조를 바꾸지 않는다.

[검증]
수정 후 아래를 확인한다.

- 로컬 확인 주소: http://127.0.0.1:8125/index.html
- 보관함 진입 가능
- 문서보관이 한 줄 목록형으로 보이는지
- 파일보관이 카드형으로 보이는지
- 갤러리가 썸네일 중심으로 보이는지
- 보관자료 미리보기 분할 구조가 사라졌는지
- 선택한 자료 하나 중심의 단일 미리보기 구조인지
- 문서/파일/갤러리 기존 자료가 사라지지 않았는지
- 새로고침 후 기존 자료가 유지되는지
- 업로드/열기/삭제/다운로드/미리보기 기능이 깨지지 않았는지
- 모바일 화면은 이번 수정 전과 달라지지 않았는지

[보고 형식]
작업 후 아래 형식으로만 보고한다.

1. 수정한 파일
2. 문서보관 변경 내용
3. 파일보관 변경 내용
4. 갤러리 변경 내용
5. 보관자료 미리보기 분할 제거 내용
6. 건드리지 않은 영역
7. 모바일 CSS 미수정 확인
8. 저장/삭제/업로드/새로고침 확인 결과
9. 로컬 확인 URL
10. 최종 git status
