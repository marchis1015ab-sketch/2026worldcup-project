# Supabase Security Migration Plan

## 목적

현재 사이트 기능을 깨지 않고, 추후 Supabase `anon` 쓰기 권한을 제거하기 위한 준비 상태를 점검한다.

이번 작업의 범위는 분석과 문서화만이며, 아래 항목은 수행하지 않았다.

- Supabase RLS 정책 변경
- 테이블 권한 변경
- 기존 저장/삭제/업로드 로직 수정
- 자동 이슈 처리 또는 자동 Resolve 수행

## 현재 확인 결과

분석 기준 경로:

- `C:\Users\march\OneDrive\문서\New project`

확인 시점:

- 2026-05-14 (Asia/Seoul)

워크스페이스 점검 결과:

- 루트에 실제 애플리케이션 소스 파일이 존재하지 않음
- 확인된 항목은 `.git` 디렉터리뿐임
- `app.js` 파일을 찾지 못함
- Git 저장소는 초기화만 되어 있고 커밋 이력(`HEAD`)이 없음
- `.git/config`에 원격 저장소 설정도 없음

따라서 요청된 핵심 작업인 아래 항목은 현재 코드 부재로 인해 확정 분석이 불가능하다.

- `app.js`에서 Supabase에 직접 `INSERT` / `UPDATE` / `DELETE` 하는 함수 전수 목록화
- 관련 테이블명 정리
- 읽기 전용 유지 가능 함수 / 서버 쓰기 API 이관 필요 함수 분류

## 수행한 확인 작업

아래 범위에서 코드 존재 여부를 확인했다.

- 현재 작업 폴더 직접 조회
- 하위 파일 재귀 탐색
- `app.js` / `*.js` / `package.json` 검색
- `supabase` 문자열 검색
- Git 인덱스 / 브랜치 / `HEAD` 상태 확인

확인 결과, 분석 대상이 되는 웹앱 소스는 현재 워크스페이스에서 발견되지 않았다.

## 요청 항목별 현재 상태

### 1. `app.js`에서 Supabase 직접 쓰기 함수 목록화

상태:

- 진행 불가

사유:

- `app.js` 파일이 없음

예정 산출물 형식:

- 함수명
- 호출 위치
- Supabase 메서드 (`insert`, `update`, `delete`, `upsert`, `rpc`)
- 대상 테이블
- 호출 조건 / 입력 데이터

### 2. 관련 테이블명 정리

상태:

- 진행 불가

사유:

- 실제 쿼리 코드가 없어 테이블명을 추출할 수 없음

예정 산출물 형식:

- 테이블명
- 사용 함수 목록
- 읽기/쓰기 용도
- anon write 제거 시 영향도

### 3. 읽기 전용 유지 가능 함수 / 쓰기 API 이관 필요 함수 구분

상태:

- 진행 불가

사유:

- 현재 저장/삭제/업로드 진입점 자체를 확인할 수 없음

판단 기준(추후 코드 확보 시 적용):

- 읽기 전용 유지 가능:
  - `select`만 수행
  - 사용자 상태 변경 없음
  - 업로드/삭제/생성/수정과 무관
- 쓰기 API 이관 필요:
  - 브라우저에서 직접 `insert` / `update` / `delete` / `upsert` 수행
  - 파일 업로드 후 메타데이터 저장
  - 사용자별 소유 데이터 생성/수정/삭제
  - 권한 체크가 클라이언트 로직에 의존

## 보안 마이그레이션 준비 관점에서의 해석

현재 워크스페이스 기준으로는 기능 분석보다 먼저 다음 선행 조건이 필요하다.

1. 실제 서비스 소스 확보
2. `app.js` 또는 동등한 엔트리 파일 확인
3. Supabase 클라이언트 초기화 코드 확인
4. 직접 쓰기 호출 전수 조사
5. 기능별 서버 API 이관 순서 정의

## 권장 다음 단계

아래 중 하나가 준비되면 즉시 후속 분석이 가능하다.

1. 실제 프로젝트 파일이 있는 폴더를 현재 작업 경로로 다시 열기
2. `app.js`가 포함된 소스를 이 저장소에 추가하기
3. 번들 결과물만 있는 경우 원본 소스 경로를 제공하기

코드가 준비되면 이 문서를 아래 구조로 업데이트하면 된다.

### 추후 문서 업데이트 템플릿

| 함수명 | 위치 | 동작 | 테이블 | 유지 방식 |
| --- | --- | --- | --- | --- |
| 예시: `savePost` | `app.js:120` | `insert` | `posts` | 서버 쓰기 API로 이관 |
| 예시: `loadPosts` | `app.js:88` | `select` | `posts` | 읽기 전용 유지 가능 |

## 결론

현재 워크스페이스에는 분석 대상 코드가 없어, 요청하신 Supabase 직접 쓰기 함수 인벤토리와 테이블 분류를 확정할 수 없다.

이번 문서는 "기능 변경 없이 준비 작업만 수행"이라는 조건을 지키면서, 현재 상태를 기록하고 후속 분석에 필요한 선행 조건을 명확히 남기는 용도로 작성했다.
